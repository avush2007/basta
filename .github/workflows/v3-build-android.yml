name: V3 Build Android APK

on:
  push:
    branches: [ v3-supermarket-platform ]
    paths:
      - 'app-source.zip'
      - 'v3-build-trigger.txt'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Extract V3 source
        run: |
          rm -rf app
          mkdir app
          unzip -q app-source.zip -d app
      - name: Apply V3.1 native mobile patches
        run: |
          for p in patches/v3.1/*.patch; do
            echo "Applying $p"
            patch -p1 -d app < "$p"
          done
          grep -q '"version": "3.1.0"' app/package.json
          node --check app/www/management.js
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: android-actions/setup-android@v3
      - name: Install dependencies
        working-directory: app
        run: npm install
      - name: Create Android project
        working-directory: app
        run: npx cap add android
      - name: Sync Android
        working-directory: app
        run: npx cap sync android
      - name: Build debug APK
        working-directory: app/android
        run: ./gradlew assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: supermarket-os-v3.1-android-apk
          path: app/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
